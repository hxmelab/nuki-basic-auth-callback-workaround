{
  "name": "nuki-basic-auth-callback-workaround",
  "version": "0.0.1",
  "description": "This project is a simple workaround (https://developer.nuki.io/t/http-api-callback-and-basic-auth/4732) to get Nuki's callback running with a secured Node-RED instance.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hxmelab",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3",
    "request": "^2.88.0"
  }
}
